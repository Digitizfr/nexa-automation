---
import Button from "../ui/Button.astro";

interface Props {
  title: string;
  subtitle: string;
  ctaLabel: string;
  ctaHref: string;
  secondaryLabel?: string;
  secondaryHref?: string;
}

const { title, subtitle, ctaLabel, ctaHref, secondaryLabel, secondaryHref } = Astro.props;
---

<section class="py-20 sm:py-28 relative overflow-hidden">
  <div class="absolute inset-0 gradient-bg"></div>
  <div class="absolute inset-0 bg-[url('data:image/svg+xml;base64,')] opacity-50"></div>

  <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-3xl sm:text-4xl lg:text-5xl font-heading text-white mb-6">
      <Fragment set:html={title} />
    </h2>
    <p class="text-lg sm:text-xl text-white/80 max-w-2xl mx-auto mb-10">
      {subtitle}
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <Button href={ctaHref} variant="secondary" size="lg" class="bg-white text-sapphire hover:bg-mystique">
        {ctaLabel}
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </Button>
      {secondaryLabel && secondaryHref && (
        <Button href={secondaryHref} variant="outline" size="lg" class="border-white/30 text-white hover:bg-white/10 hover:text-white">
          {secondaryLabel}
        </Button>
      )}
    </div>
  </div>
</section>
